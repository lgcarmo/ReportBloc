# Arquivo de exemplo de configuração
# Copie este arquivo para .env e configure suas variáveis

# Chave secreta para sessões Flask (GERE UMA CHAVE ÚNICA!)
SECRET_KEY=sua-chave-secreta-muito-segura-aqui-mude-esta-chave

# Configuração do banco de dados
# SQLite (padrão)
DATABASE_URL=sqlite:///reports.db

# PostgreSQL (opcional)
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/reports

# Configurações LDAP (opcional - descomente para usar)
# LDAP_URL=ldap://seu-servidor-ldap:389
# LDAP_BASE_DN=dc=empresa,dc=com
# LDAP_BIND_USER=cn=admin,dc=empresa,dc=com
# LDAP_BIND_PASSWORD=sua-senha-ldap

# Configurações de produção (opcional)
# DEBUG=False
# HOST=0.0.0.0
# PORT=5000 